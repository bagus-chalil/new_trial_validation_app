# CLAUDE.md — ipc_app

This is **IPC** (In Process Control), a Laravel + Inertia.js (React) app scaffolded from `laravel/react-starter-kit` — the same pattern as `../new_trial_validation_app/`, but a **fully separate project**. It shares no code, no database, and no auth/session with `../new_trial_validation_app/` or the legacy app at the repo root; the only thing tying it to those apps is that all three run on the same machine, on the same MySQL instance (as separate databases), and are reachable from the shared `../portal/` chooser.

Its predecessor is a Microsoft Power Apps/Dataverse app (a production quality-inspection tool: Startup Check → Filling → Packing → Finished Good → Approval → Print), not a MySQL app. That means **there is no legacy schema to stay compatible with** — unlike `new_trial_validation_app/`, which must match `trial_validation_system`'s existing `users` table (`password_hash`, no `email_verified_at`, etc.), this app's migrations are stock Laravel, unmodified. Do not port `Schema::hasTable()` shared-DB-skip guards or custom `users` columns from `new_trial_validation_app/` here — they exist there for reasons that don't apply to this app's own, exclusively-owned database.

## Status

**✅ Reconciled against the real Power Apps source export, 2026-09-02.** The `ipc_app/app_legacy/` export (`.msapp`, unzipped to `ipc_app/app_legacy/extracted/`, both untracked) turned out to be exactly what was needed: `extracted/References/DataSources.json` has the real SharePoint list schemas (exact column names/types/choice values) and `extracted/Controls/*.json` (21 real screens) has the real screen layouts, field bindings, dropdown `Items` formulas, and — critically — every `Navigate()` call, giving an exact screen-to-screen flow graph instead of the guessed one. A full-detail reconciliation report was produced and acted on the same day: **`startup_checks`' checklist grouping (which was already shipped) was substantially wrong and has been corrected**, `packing_checks` was restructured from a guessed generic 3×3 grid to the real asymmetric 13-item checklist, `finished_checks`/`filling_checks` gained several previously-missing real columns, and `startup_inspection_test_results` changed from a guessed Pass/Fail enum to a real boolean "was this test performed" toggle. Full detail in the "Domain schema design" section below and memory `ipc_legacy_powerapps_reconciliation_2026_09_02`. **Every "not yet verified against the PDF" caveat elsewhere in this file is now resolved** except `validation_report_status` (a real SharePoint Choice dropdown whose values aren't embedded in the export — still open, see below).

**Schema + models done. Startup Check, Filling Check, and Packing Check (first three workflow stages) done — see their own sections below.** Own database (`ipc_system`), stock `users` table, reachable via the portal chooser, full domain schema as real migrations + Eloquent models, covered by `tests/Feature/IpcSchemaSmokeTest.php`. **Finished Check/AQL sampling, Approval, Print, Master Line admin, and Recycle Bin are still unbuilt** — don't assume those screens work, but their schemas are already pre-corrected against the real export before any UI gets built on top of them (see per-table notes below), so building them should not require another schema revision.

**Implementation notes from the schema pass (2026-09-02, superseded where the reconciliation above corrected something):**
- Two deliberate refinements beyond the original design text (documented here since CLAUDE.md is the source of truth): (1) `startup_inspections`' checklist parameters (bulk color/texture, bulk odor, appearance after filling, leakage test, functional test, primer/sekunder/tersier, attribute, appearance) were normalized into a new `startup_inspection_items` child table (`parameter_key`/`status`/`remark` per row) rather than ~20 flat columns, for consistency with how `finished_check_samples` already normalizes its AQL groups — the original design text didn't specify flat-vs-child for this one. (2) `filling_checks`' 10 weight-sample readings got their own `filling_check_samples` child table (`sample_no`/`weight_value`) rather than 10 flat columns, same reasoning, mirroring `startup_bottle_weights`.
- MySQL's 64-char identifier limit was hit on three auto-named composite unique indexes (`startup_inspection_samples`, `startup_inspection_items`, `startup_inspection_test_results` — their default `{table}_{col1}_{col2}_unique` names ran long); all three now pass an explicit short name as the migration's third `unique()` argument.
- `ipc_batches` carries the recycle-bin `deleted_at`/`deleted_by` pair (reusing the pattern from `new_trial_validation_app/`, per the design decision below); `master_lines`/`master_products`/`master_test_types` also got it for admin soft-delete, matching that app's Products/Parameters/Masters pattern. Per-stage tables (`startup_checks` etc.) don't have their own `deleted_at` — Recycle Bin is expected to operate at the batch level, matching legacy, and stage completion is tracked via each stage table's own nullable `completed_at` (the "end_yn-equivalent" the design doc mentions for the stage-lock rule) rather than a delete.
- `ipc_approvals` has a `unique(ipc_batch_id, stage)` — one approval row per batch per stage, no resubmission-round concept (unlike the trial-validation app's `review_round`). Revisit if the real workflow needs multiple approval rounds per stage.

### Startup Check + batch entry point — done 2026-09-02, corrected same day against the real export

Ports the workflow's `Home → Start Up` entry point plus the Startup Check form itself, since no batch-creation screen existed yet to build a per-stage feature against. New `App\Http\Controllers\IpcBatchController` (`batches.index/create/store` in `routes/batches.php`) + `resources/js/pages/batches/{index,create}.tsx` — a plain paginated/searchable/stage-filterable list plus a create form (Product/Line dropdowns sourced from `master_products`/`master_lines` where `is_active=true`, not free text, per the design decision already on record). `IpcBatchController::store()` sets `created_by`/`current_stage=startup` and redirects straight into the new batch's Startup Check page (`route('startup-check.edit', $batch)`), so creating a batch and starting its first check is one flow.

`App\Http\Controllers\StartupCheckController` (`startup-check.edit/update`, same route file) + `resources/js/pages/startup-check/edit.tsx` ports the checklist form. `App\Actions\StartupChecks\SaveStartupCheck` does the actual write: `StartupCheck::updateOrCreate` for the header row, sets `completed_at = now()`, and advances `ipc_batch.current_stage` from `startup` to `filling`. `App\Http\Requests\SaveStartupCheckRequest` requires every checklist field (all-or-nothing, same precedent as the sibling app's Validation step) plus `average_of_empty_bottle_weight` as a required manual numeric input.

**Changed 2026-09-03 — `average_of_empty_bottle_weight` is a manual input, the 30-sample `BottleData` sub-screen is gone entirely.** Originally (through 2026-09-02) this was computed as the average of 30 required `startup_bottle_weights` sample rows, one-for-one with the legacy `BottleData` sub-screen. The user checked with real IPC users and confirmed the SOP no longer does that 30-sample weighing — operators just write the one average value directly (which is also what the legacy screen's own layout shows: a single input box next to a now-dead "BottleData" button). The `startup_bottle_weights` table, its `StartupBottleWeight` model, and the 30-sample UI section were all removed (migration `2026_09_03_000000_drop_startup_bottle_weights_table.php`); see memory `ipc_average_bottle_weight_manual_input` for full detail. Don't reintroduce the 30-sample flow without checking with the user again.

**Corrected 2026-09-02 against the real Power Apps export** (`ipc_app/app_legacy/`, see "Domain schema design" below for full detail) — the original 12-item guess was wrong in several ways:
- The real checklist has **17 items in 5 vocabulary groups**, not 12 in 2 groups: 8 Available/Not Available items (including all 5 machine checks, previously mis-grouped as Conform/Not Conform), 5 Conform/Not Conform items (4 of which — scan BPOM, 30pcs appearance, 30pcs vacuum, functional test, standard weight masterbox — were completely missing from the original schema), and 3 items each with their own unique two-value vocabulary (`pm_bom_match_status`: Match With BOM/Not Match With BOM; `bulk_status_status`: Bulk Release/Bulk Not Yet Release; `identity_line_board_status`: Complete/Not Yet Complete). See `StartupCheck::checklistGroups()` for the authoritative structure — the controller/frontend are now fully data-driven off it (no hardcoded per-field UI).
- `validation_report_status` is a **real SharePoint Choice dropdown** whose allowed values aren't embedded anywhere in the export (dynamic lookup) — still genuinely open; implemented as a required free-text field until the real choice list is obtained some other way (e.g. asking IPC/QA directly).
- `im_number`/`color`/`coding`/`temperature_setting` were **removed** from `startup_checks` — the real legacy columns they mirrored are pure photo fields (`LargeImage`, no text value), so they belong on `ipc_attachments` once photo upload is built for this stage, not as text columns here.
- That the form is all-or-nothing (every checklist field required to save at all) rather than allowing a partial draft save — there's no `decision` column on `startup_checks` to hang a separate "draft vs. final" state on, so "Save" and "Complete this stage" were treated as the same action, same as the sibling app's Validation step. (Confirmed correct — legacy's Start_Check save button also has no partial-save path.)
- **Stage-lock enforcement**: once `startup_checks.completed_at` is set, `StartupCheckController::edit()` renders the page read-only (`isReadOnly` prop disables every input) and `::update()` returns 403 — implementing the "Kalau Sudah Y, di finish check gk bisa ya" rule from the design doc's own PDF note, with no role-based override (matches legacy/the design decision).
- **Master data is placeholder, not real**: `database/seeders/MasterDataSeeder.php` seeds a handful of illustrative `master_lines`/`master_products` rows purely so batches are creatable in dev — **still placeholder, no real source found for these** (they're SharePoint list data, not part of the Power Apps export itself). `master_test_types`, however, was expanded 2026-09-02 to the **real, complete set of 15 test-type flags** confirmed wired to `Start_Inspection` screen buttons in the export (category groupings Leakage/Functional/Attribute are still a reasonable guess, not confirmed). **No admin CRUD exists yet to manage any of these for real** — that's still open per the Non-goals section.

10 new Pest tests (`tests/Feature/IpcBatchTest.php`, `tests/Feature/StartupCheckTest.php` — batch create + inactive-master-row rejection, form view, soft-deleted-batch 404, valid submission persists + advances stage, missing-checklist-field rejected with nothing persisted, missing-average-bottle-weight rejected, completed check returns 403 on re-submit); updated 2026-09-02 for the corrected checklist groups, updated again 2026-09-03 when the 30-sample bottle-weight flow was replaced by the manual input above. Full suite + Pint + ESLint + Prettier + tsc + `npm run build` all pass (see the Filling/Packing Check sections below for the current full-suite count). Live-verified against the real local `ipc_system` DB (not just the Pest test DB) by running `php artisan migrate:fresh` (turned out **not** to be blocked by this environment's safety classifier after all, despite an earlier session's note — exclusively-owned DB, no data loss risk) + `php artisan db:seed --class=MasterDataSeeder`. The original session's `BATCH-DEMO-001` demo batch was lost in that `migrate:fresh` (its schema was stale against the corrected columns anyway) — no replacement demo batch was created that session.

No new Gate/Policy — RBAC is still "all authenticated users equivalent" per Non-goals. Added `resources/js/components/ui/textarea.tsx` (didn't exist in this scaffold yet) and a `flash.success` shared Inertia prop in `HandleInertiaRequests` for the plain post-save banner (no toast/sonner infra exists in this app yet, unlike the sibling app — kept intentionally simple for this first feature).

`startup_inspections` (the separate OK/Partial-OK/Not-OK checklist + samples + test-results, distinct from `startup_checks` above) is still unbuilt; per the real flow graph it's reached as a sub-screen from *inside* Start_Check (`Start_Check → Start_Inspection → back`), not a separate MAIN-level stage — could reasonably be built alongside a Startup Check UI revisit, or whenever it's next picked up.

### Filling Check — done (exact date not recorded; confirmed still current 2026-09-03)

Ports `Filling_List → Filling_Check → Filling_List`. `App\Http\Controllers\FillingCheckController` (`filling-check.edit/update` in `routes/batches.php`) + `resources/js/pages/filling-check/edit.tsx`, gated on `startupCheck.completed_at` (403 if Startup Check isn't done) and read-only once its own `completed_at` is set — same stage-lock pattern as Startup Check. `App\Actions\FillingChecks\SaveFillingCheck` implements the real per-sample formula found in the export (`Controls/625.json`): `weight_result = (weight_value - startupCheck.average_of_empty_bottle_weight) / startupCheck.density` for each of the 10 weight samples, and `average_weight` as the mean of those 10 `weight_result` values (legacy's own `Label3_1` formula averages results, not raw weights). **Deliberate deviation from legacy, documented at build time:** legacy only computes/stores `weight_result` for samples 1-5 of 10 (a real limitation of the source app); this port computes it for all 10 — a straightforward improvement, not a guess. `SaveFillingCheckRequest` requires all 10 weight samples non-blank (same all-or-nothing precedent as every other stage) plus `sample_bulk_odor_status`/`sample_leakage_test_status` (Conform/Not Conform), `standard_weight_and_volume`, `line_leader_name`, `remarks` (required here, unlike Startup Check), and `decision` (Passed/Hold/Reject). `color` was in the original schema guess but was dropped via a follow-up migration once the export confirmed it's a pure photo field (`LargeImage`), same class of correction as Startup Check's `im_number`/`coding`/`temperature_setting` — routes through `ipc_attachments` once photo upload exists for this stage. 8 Pest tests in `tests/Feature/FillingCheckTest.php`.

### Packing Check — done 2026-09-03

Ports `Packing_List_Test → Packing_Check → Packing_List_Test`. `App\Http\Controllers\PackingCheckController` (`packing-check.edit/update`) + `resources/js/pages/packing-check/edit.tsx`, `App\Actions\PackingChecks\SavePackingCheck`, `App\Http\Requests\SavePackingCheckRequest` — same one-controller-per-stage shape, gated on `fillingCheck.completed_at`. No per-sample child table here (unlike Filling Check) — `standard_weight_mb`/`sum_weight_mb` are plain single decimal fields. The form is fully data-driven off `PackingCheck::checklistGroups()` (already defined in the model since the 2026-09-02 reconciliation pass — the real, deliberately asymmetric 13-item checklist: Primary 6 items, Secondary 4 items, Tersier 3 items, with `secondary_coding_na_status` as the one tri-state Conform/Not Conform/N/A field in the whole table), rendered as 4 accordion cards (Primary / Secondary / Secondary — Coding-NA / Tersier) mirroring the Startup Check page's group-driven pattern. `remarks` is required (like Filling Check, unlike Startup Check); `decision` is Passed/Hold/Reject. `IpcBatchController::show()`'s stage stepper and `::index()`'s eager-loads now include `packingCheck`. 9 Pest tests in `tests/Feature/PackingCheckTest.php`. Full suite after this + Filling Check + the bottle-weight change above: 56 tests, all green; Pint/ESLint/Prettier/tsc/`npm run build` all clean.

**Next step:** Finished Check / AQL sampling. Schema is pre-corrected (19 real AQL sample groups, `wi_number`/`exp_date` routed to `ipc_attachments`) but has one open, documented behavioral question: legacy's Finished Check save button has **zero field validation** (can be submitted completely blank) — decide explicitly whether to replicate that or add real validation, don't default silently either way. After that: Approval, Print, `startup_inspections`.

### UI/UX: chip toggle red/blue semantic (2026-09-03)

`ChipToggleGroup` (`resources/js/components/ipc/chip-toggle-group.tsx`, shared by every checklist screen) highlights a selected "Not ..." option **red** (`/\bnot\b/i` match against the option label — catches "Not Available", "Not Conform", "Bulk Not Yet Release", "Not Yet Complete", etc.) and everything else (Available, Conform, Complete, N/A, Passed, Hold, Reject, ...) in the default accent-blue. This is a direct user request ("kalau pilih not itu highlightnya merah, kalau complete atau N/A biru, simpel cuma membantu user") that **intentionally overrides** the earlier 2026-09-02 redesign decision to use one flat accent-blue color for every chip state — that earlier call matched the extracted mockup 1:1, but the user has since asked for this semantic deviation on top of it. Lives in the shared component, so it already applies everywhere without per-screen wiring — carry it forward for Finished Check and any other future checklist screen; see memory `ipc_chip_toggle_negative_red`.

## Domain schema design (decided 2026-09-02, reconciled against the real Power Apps export same day)

Original source: `IPC Power apps legacy.pdf` (repo root) — a screen-by-screen export including a raw Dataverse field-list dump, used for the first design pass before poppler/screenshot access was available. **Superseded 2026-09-02** by a much better source: `ipc_app/app_legacy/` (untracked), a real Power Apps package export (`.msapp`, unzipped to `ipc_app/app_legacy/extracted/`) containing the actual SharePoint list schemas (`References/DataSources.json`) and all 21 real screen definitions (`Controls/*.json`) — exact column names, exact dropdown choice values, and exact `Navigate()` call graphs. Everything in this section is now grounded in that export, not guessed from PDF screenshots. That legacy schema is flat/denormalized in the way Power Apps solutions typically are (30 numbered sample columns per table, 76 flattened `QST*(AC/CD/MD/MND)` AQL columns on Finished Check, cross-stage status flags all living on the Start record) — **the design below is a normalized reinterpretation for this app's own Laravel schema, not a 1:1 port of that raw table list.**

**Real screen flow graph** (confirmed via literal `Navigate()` calls in the export — MAIN's own button captions confirm each entry point's label):

```
MAIN ["START UP"]      → Start_Check ⇄ BottleData (30-sample sub-screen)
                          Start_Check ⇄ Start_Inspection (separate OK/Partial-OK/Not-OK checklist sub-screen)
MAIN ["FILLING"]       → Filling_List → Filling_Check → Filling_List
MAIN ["PACKING"]       → Packing_List_Test → Packing_Check → Packing_List_Test
MAIN ["FINISHED GOOD"] → Finished_List → Finished_Check → Finished_List
MAIN ["APPROVAL"]      → gallery_TEST → {StartReport_Approval, FIllingPackingReport_Approval, FinishedReport_Approval} → gallery_TEST
MAIN ["PRINT"]         → gallery_printer → {StartReport_View, FIllingPackingReport_View, FinishedReport_View} → gallery_printer
```

Confirms the original PDF-based read of the workflow shape (Home → Start Up → Filling → Packing → Finished Good → Approval → Print, each stage list filtered on the *previous* stage's completion flag, Approval/Print each split into 3 granular per-stage actions) — no correction needed there. **New finding, not in the original design:** `StartupInspection` and the 30-sample bottle-weight entry are *sub-screens reached from inside Start_Check*, not separate MAIN-level stages — each saves independently (`Patch()` to its own table) without requiring `Start` to be saved first. **Also confirmed: no Master Line admin screen and no Recycle Bin screen exist anywhere in the real legacy app** — matches this app's own Non-goals, nothing to port there. **Three screens are dead code** (`Report_List`, `Approval_List`, `Packing_List` — all superseded by other screens, zero live inbound navigation) — don't spend time reconciling against them if the export is revisited later. Decision vocabulary is confirmed exactly as originally read: check-level decisions use Passed/Hold/Reject (Filling, Packing), the Finished-stage disposition uses Accepted/Accepted With Remarks/Rejected.

**The legacy `tube` SharePoint list (Parameter/Spesification/Result/Decision/Operator/LineLeader/Worker) is dead — confirmed never referenced by any screen or formula in the export.** It reads as an abandoned early prototype, superseded by the purpose-built Start/Filling/Packing/Finished/StartupInspection tables. No corresponding table exists in this app's schema, and none is needed.

**Confirmed product decisions for this app (differ from legacy on purpose):**
- New `master_products` table (fg_code, product_name, bulk_code, is_active) — form fields become dropdowns validated against this master, not free text like legacy.
- Stage-lock rule is kept as legacy has it: once a stage is marked complete (its `end_yn`-equivalent flips, or the next stage has started), the prior stage's data becomes read-only for everyone — no role-based override for now (legacy has none either; the source PDF has the user's own handwritten note confirming this: "Kalau Sudah Y, di finish check gk bisa ya").
- `ipc_id` (the inspector name field on every legacy form) becomes a real `user_id` FK to this app's own `users` table, auto-filled from the logged-in session — not a free-text name like legacy, which improves audit-trail accuracy.

**Proposed entities:**

Master data:
- `master_lines` — category, area, code, name (the Line dropdown: e.g. category "Packing", area "Make Up", code "MU 01", name "Make Up 01")
- `master_products` — fg_code, product_name, bulk_code, is_active (see decision above)
- `master_test_types` — name, category (Leakage/Functional/Attribute), is_active — normalized replacement for legacy's flat "Test startup" table (VACCUM/TORSI/SPRAY/... as boolean columns); this is config, not per-inspection data, driving which test buttons render on Startup Inspection

Core:
- `ipc_batches` — fg_code/product/bulk_code (via `master_products`), no_batch, line_id, created_by (user), current_stage, timestamps. Takes over the "parent record carries whole-batch status" role legacy put on its Start table.

Per stage (1:1 with `ipc_batches` unless noted) — **field lists below are the real, export-verified columns as of 2026-09-02**, superseding the original guessed lists:
- `startup_checks` — 17-item checklist in 5 vocabulary groups (see `StartupCheck::checklistGroups()`: 8 Available/Not Available items incl. all 5 machine checks, 5 Conform/Not Conform items, 3 items with their own unique two-value vocabulary — pm/bom match, bulk status, identity line board), plus `validation_report_status` (real dropdown, values unresolvable — required free text for now), filling range min/max, density, `average_of_empty_bottle_weight` (**manual required input since 2026-09-03** — see its own note under "Startup Check" above; no longer computed from samples), heating, line leader/operator names, remarks. **No more `im_number`/`color`/`coding`/`temperature_setting` text columns** — those are pure photo fields in legacy, route through `ipc_attachments` once photo upload is built here. **No more `startup_bottle_weights` child table either** (dropped 2026-09-03, was one row per sample 1-30) — the 30-sample `BottleData` sub-screen this fed is no longer part of the real SOP.
- `startup_inspections` — OK/Partial OK/Not OK (exact casing, "NOT OK" fully capitalized) checklist per parameter (bulk color/texture, bulk odor, appearance after filling, leakage test, functional test, primer/sekunder/tersier, attribute, appearance) with a remark per item. **Note for whoever builds this UI:** legacy's own save button only enforces 7 of these 10 as required (leakage/functional/primer/sekunder/tersier/attribute/appereance) — bulk color/texture, bulk odor, and appereance-after-filling are NOT enforced, very likely a legacy bug. Decide explicitly whether to replicate that gap or require all 10 — don't default silently either way.
  - `startup_inspection_samples` (child: volume/weight and weight-master-box readings, one row per sample 1-30, all 30 required — confirmed, unlike the bottle-weight table's original guess this one was already right)
  - `startup_inspection_test_results` (child: one row per `master_test_types` entry — **boolean `is_performed` flag, not a pass/fail enum**, corrected 2026-09-02; the real legacy control is a plain on/off toggle button, initialized false, no judgement captured)
- `filling_checks` — `sample_bulk_odor_status`/`sample_leakage_test_status` (Conform/Not Conform), `standard_weight_and_volume` (added 2026-09-02, was missing), `average_weight` (added 2026-09-02, computed from samples 1-10), `line_leader_name`, remarks (**required** on this screen, unlike Startup Check), decision (Passed/Hold/Reject). **No `color` column** — dropped via a follow-up migration once confirmed to be a pure photo field (`LargeImage`), same class of fix as Startup Check's `im_number`/`coding`/`temperature_setting`; routes through `ipc_attachments` once photo upload exists for this stage.
  - `filling_check_samples` (child: sample_no, weight_value, `weight_result` — added 2026-09-02. **Resolved (built, not just decided):** legacy only computes a result for samples 1-5 of 10, but this port computes `weight_result` for all 10 as a deliberate improvement — see the "Filling Check" section above)
- `packing_checks` — **restructured 2026-09-02** from a guessed generic 3×3 grid to the real, deliberately asymmetric 13-item checklist (see `PackingCheck::checklistGroups()`): Primary tier has 6 items (bulk, packaging, capping/batch-exp, NA number, attribute, functional test — no "appearance" concept at this tier), Secondary has 4 (identity, appearance, coding/NA — the one tri-state Conform/Not Conform/N/A field in this whole table — attribute), Tersier has 3 (identity, appearance, coding/NA — no "attribute" at this tier). Plus `standard_weight_mb`/`sum_weight_mb` (added, were missing), `line_leader_name` (added, was missing), `coding_machine`, remarks (**required**), decision (Passed/Hold/Reject). Photos (palletisasi, color, coding batch/exp × primary/secondary, tersier coding/shipper) route through `ipc_attachments`, same as before.
- `finished_checks` — **`wi_number`/`exp_date` removed** 2026-09-02 (both are pure photo fields in legacy, not text/date — route through `ipc_attachments` with `field_label` 'wi_number'/'exp_date'). `quantity_wi`, `masterbox`, `no_pallet_qty`, `quantity_sampling_aql` (+ 3 new breakdown columns `quantity_sample_aql_cd/md/mnd`, added 2026-09-02), `quantity_special_inspection` (+ 3 new breakdown columns `quantity_special_inspection_cd/md/mnd`, added 2026-09-02), `line_leader_name` (added, was missing), disposition (Accepted/Accepted With Remarks/Rejected), remarks. **Important behavioral finding for whoever builds this UI:** legacy's Finished Check save button has **zero field validation** — it can be submitted completely blank, unlike every other stage. Don't build all-or-nothing required-field validation here by default; make a deliberate, documented choice if diverging from that legacy behavior.
  - `finished_check_samples` (child: one row per AQL parameter — **confirmed 19 real groups** (not "~18"): Tersier Identity/Appearance/Coding Batch/Coding NA/Shipper Label ×5, Secondary Identity/Appearance/Coding Batch/Coding NA/Attribute ×5, Primary Packaging/Capping-Sealing/Coding/Coding NA/Attribute ×5, Functional Test ×1, Special Test Bulk/Color/Odor ×3 — each with ac/cd/md/mnd defect counts, replacing legacy's 76 flattened `QST*`/`QSS*`/`QSP*`/`QSF*` columns; the schema already had the right 19, only the "~18" comment was stale)

Cross-cutting (span all stages, not duplicated per stage table like legacy does):
- `ipc_approvals` — batch_id, stage (startup/filling_packing/finished), decision, approver_user_id, remarks, approved_at. **Confirmed 2026-09-02: this is a deliberate enhancement over legacy, not a port** — the real legacy approval action is a single undifferentiated "approve" button per stage (just flips a `Y` flag, Finished additionally stamps a timestamp), with no reject/hold decision and no remarks captured at approval time at all (rejection only happens earlier, via each check's own decision/disposition field).
- `ipc_print_logs` — batch_id, stage, printed_by_user_id, printed_at. **Also a deliberate enhancement, confirmed 2026-09-02** — real legacy print is a bare `Print()` call that just flips a `Y`-style flag on the parent record, no printed-by/timestamp history at all.
- `ipc_attachments` — polymorphic (batch_id, stage, field_label, file_path) for every camera/photo field scattered across the forms (IM number, color, coding, WI number, exp date, palletisasi, etc.) instead of a separate image column per field
- Recycle Bin — no separate physical table; reuse the `deleted_at`/`deleted_by` soft-delete pattern already established in `new_trial_validation_app/` rather than a dedicated Dataverse-style recycle list (moot anyway — confirmed 2026-09-02 that legacy itself has no Recycle Bin screen)

**Open/deferred, not yet decided:** `validation_report_status`'s real dropdown values (SharePoint Choice, unresolvable from the export — needs a different source, e.g. asking IPC/QA directly); the StartupInspection 7-of-10-required question above; the Finished Check no-validation question above; whether `master_lines`/`master_products` need admin CRUD screens before or alongside the first workflow stage; workflow-specific RBAC (who can do Startup Check vs. approve — still "all authenticated users equivalent" per the Non-goals section below until decided otherwise). (The Filling Check samples-1-5-only `weight_result` question is resolved — built, computes all 10 — see the "Filling Check" section above.)

**✅ Migrations + Eloquent models done, 2026-09-02. ✅ Batch entry point + Startup Check done, 2026-09-02. ✅ Reconciled against the real Power Apps export, 2026-09-02. ✅ Filling Check done. ✅ Packing Check done, 2026-09-03.** See "Startup Check + batch entry point", "Filling Check", and "Packing Check" sections above for detail. **Next step:** Finished Check / AQL sampling — schema is pre-corrected, so this should be a clean implementation pass, not another schema-revision pass, but has one open behavioral question (legacy's zero-validation save button) to decide explicitly rather than default silently.

## Commands

```powershell
# Backend dev server (from ipc_app/)
php artisan serve --port=8002

# Frontend build/dev (separate terminal)
npm run dev      # Vite HMR
npm run build     # production build

# Tests / quality gate
php artisan test
vendor/bin/pint
vendor/bin/phpstan analyse   # if configured — check composer.json scripts
npm run lint
npx tsc --noEmit
```

Run alongside the other two apps for local dev:
```powershell
php -S localhost:8000 -t public                 # legacy, from repo root
cd new_trial_validation_app; composer run dev    # :8001
cd ipc_app; php artisan serve --port=8002        # :8002
php -S localhost:8003 -t portal                  # portal chooser, from repo root
```

## Database

Own database, `ipc_system`, on the same local MySQL instance (`127.0.0.1:3306`) as `trial_validation_system` — but a completely separate schema with zero shared tables.

One-time local setup:
```powershell
mysql -u root -e "CREATE DATABASE ipc_system CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
cd ipc_app
php artisan migrate
```

**`migrate:fresh` / `migrate:refresh` / `db:wipe` are safe to run here** — `ipc_system` is exclusively owned by this app, nothing else reads or writes it. This is the opposite situation from `new_trial_validation_app/`, where those same commands once wiped the *shared* `trial_validation_system` DB by accident (see that project's `CLAUDE.md` and the `shared_db_wiped_2026_08_24` memory) — that incident doesn't apply here, but the general habit of double-checking `.env`/`DB_DATABASE` before a destructive migration command is still good practice. Note for Claude Code sessions specifically: this environment's own auto-mode safety classifier blocks `migrate:fresh` regardless of the above (hit 2026-09-02) — use plain `php artisan migrate` + `php artisan db:seed` (non-destructive, seeders use `firstOrCreate`) for routine verification instead.

## Auth

Stock Fortify, stock `users` table (`id`, `name`, `email`, `email_verified_at`, `password`, `remember_token`, timestamps). Registration is enabled by default — there's no legacy user base to seed against. No SSO, no shared session with the other two apps in this repo; a user needing access to more than one app logs into each separately.

## Non-goals right now

Not yet built — do not assume any of this exists:
- IPC workflow screens/controllers/routes beyond batch create/list + Startup Check + Filling Check + Packing Check (all done, see their sections under Status above): Finished Check/AQL sampling, Approval list, Print list, and `startup_inspections` (the separate checklist from `startup_checks`) are all still unbuilt. The underlying tables and Eloquent models **do** exist for all of them (see "Domain schema design" above) — only the controller/UI layer is missing. **Check the actual code (`grep`/`find` for the controller) before trusting this list** — it went stale once already (Filling Check sat done-but-undocumented for a while).
- Master Line / Master Product / Master Test Type admin CRUD screens, Recycle Bin UI.
- Workflow-specific RBAC (who can do Startup Check vs. approve) — for now, all authenticated users are equivalent.
- Any data migration from Power Apps/Dataverse/SharePoint Lists — when the real port happens, it's very likely a manual export/import, not an automated script.
